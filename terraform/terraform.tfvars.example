# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your actual values

# AWS Configuration
aws_region = "eu-west-1"  # Change to your preferred region

# Project Configuration  
project_name = "op1-finder"  # Change if you want a different name

# Telegram Configuration
# Get these from @BotFather on Telegram
telegram_token   = "1234567890:ABCDEFghijklmnopQRSTUVwxyz123456789"
telegram_chat_id = "123456789"

# Schedule Configuration
# How often to run the agent (EventBridge schedule expression)
schedule_expression = "rate(2 hours)"  # Every 2 hours
# Examples:
# "rate(1 hour)"           # Every hour
# "rate(30 minutes)"       # Every 30 minutes
# "cron(0 9-17 ? * 1-5 *)" # Every hour from 9 AM to 5 PM, Monday to Friday 